rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for Attendance QR System (كنيسة مارمينا العجايبي)
    // Allows instant student and attendance saving & synchronization
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
